# shadowfile
The Shadow File Encryptor (CLI) A command-line tool that "hides" files by encrypting them and renaming them to look like boring system files.

## Feats
- A **_Self-Destruct_** timer. If the wrong password is entered certain times, the registry (and the encrypted data) is wiped.
